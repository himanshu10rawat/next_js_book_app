import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["img.icons8.com"], // Add img.icons8.com to the domains array
  },
};

export default nextConfig;
